# Freezerbot runtime dependencies
# Install on Raspberry Pi with:
#   pip install --break-system-packages -r requirements.txt

# Wake word detection (open-source, no account required)
openwakeword

# Numeric audio processing
numpy>=1.26

# Audio I/O
pyaudio==0.2.14
# pygame and PyQt6 must be installed via apt on Raspberry Pi (arm64 pip builds fail):
#   sudo apt-get install -y python3-pygame python3-pyqt6

# Speech-to-text (Groq Whisper API)
groq==0.9.0

# Natural language / intent (Gemini)
google-generativeai==0.7.0

# Text-to-speech
gTTS==2.5.1
pyttsx3==2.90

# UI — PyQt6 installed via apt (see note above)

# Fuzzy search
rapidfuzz==3.9.0

# HTTP (used by gTTS and connectivity checks)
requests==2.32.0
# Pin httpx to avoid groq SDK incompatibility with httpx>=0.28 (proxies kwarg removed)
httpx<0.28.0
